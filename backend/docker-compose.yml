services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: saarthi
    ports:
      - "5432:5432"
    volumes:
      - saarthi_pgdata:/var/lib/postgresql/data

volumes:
  saarthi_pgdata: